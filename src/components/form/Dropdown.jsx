
import React from "react"
import Select from "react-select"

const Dropdown = (props) => {
    return (
        <Select className="text-left text-gray-900 font-medium" {...props} />
    )
}

export default Dropdown
